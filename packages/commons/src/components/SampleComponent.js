import React from "react";
import {Text, View} from "react-native";
export const SampleComponent = () => {
    return <View style={{flex: 1, alignItems: 'center'}}>
        <Text>React native sample component - Reusable in mobile and web</Text>
    </View>

}
